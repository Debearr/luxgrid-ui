# 🤖 Cursor Agent Templates — Copy & Paste Ready

**Instant workflow activation for any Cursor session**

---

## 🚨 Emergency Templates

### Critical Security Fix
```txt
EMERGENCY: Security VOAI buckets only. Apply patches immediately + security audit + fast-track PR with `security` label + skip non-critical tests.
```

### Production Hotfix
```txt
HOTFIX: Critical VOAI buckets. Minimal diffs + emergency build + hotfix branch `hotfix/cursor-emergency-fix` + immediate deploy.
```

---

## 🎯 Specialized Templates

### Performance Optimization
```txt
Performance Mode: VOAI Performance buckets only. Apply optimizations + bundle analysis + lighthouse testing + performance PR with metrics.
```

### Accessibility Compliance
```txt
A11y Mode: VOAI Accessibility buckets only. Apply WCAG AA+ fixes + axe-core testing + accessibility PR with compliance report.
```

### Code Quality Refactor
```txt
Refactor Mode: VOAI Console Errors only. Clean up code issues + type safety + linting fixes + refactor PR with quality metrics.
```

### Network & API Fixes
```txt
Network Mode: VOAI Network Failures only. Fix API issues + timeout handling + retry logic + network PR with test coverage.
```

---

## 🔧 Build Variations

### Quick Build (Skip Tests)
```txt
Fast Mode: Apply VOAI buckets + quick build (skip tests) + draft PR for rapid iteration.
```

### Full Validation Build
```txt
Full Mode: Apply VOAI buckets + complete test suite + security audit + accessibility check + production-ready PR.
```

### Staging Deploy
```txt
Staging Mode: Apply VOAI buckets + build + deploy to staging branch `staging/cursor-fixes` + staging validation.
```

---

## 📋 Custom Workflows

### Multi-Agent Coordination
```txt
Coordinated Mode: Parse VOAI buckets. Agent 1 handles [Console Errors][Performance]. Agent 2 handles [Accessibility][Security]. Agent 3 handles [Network Failures]. Merge all fixes into single PR.
```

### Incremental Rollout
```txt
Incremental Mode: Apply 25% of VOAI buckets + build + feature flag deployment + monitor metrics + apply next 25% if stable.
```

### Rollback Ready
```txt
Rollback Mode: Apply VOAI buckets + build + create rollback branch `rollback/pre-cursor-fixes` + deploy with automatic rollback triggers.
```

---

## 🎨 Brand-Aware Templates

### Brand Compliance
```txt
Brand Mode: Apply VOAI buckets + brand validation + design system compliance + brand-approved PR with visual regression tests.
```

### Design System Update
```txt
Design Mode: Apply VOAI buckets affecting design tokens + Storybook update + visual component testing + design system PR.
```

---

## 🔍 Analysis Templates

### Impact Assessment
```txt
Analysis Mode: Parse VOAI buckets + estimate change impact + dependency analysis + risk assessment report. NO CODE CHANGES.
```

### Diff Preview Extended
```txt
Extended Preview: Parse VOAI buckets + detailed diffs + file dependency map + testing strategy + manual application guide.
```

---

## ⚙️ Configuration Templates

### Environment-Specific
```txt
Dev Mode: Apply VOAI buckets + development build + local testing + dev branch PR.
Prod Mode: Apply VOAI buckets + production build + full test suite + main branch PR.
```

### Team Handoff
```txt
Handoff Mode: Apply VOAI buckets + comprehensive comments + documentation updates + team review PR with detailed change log.
```

---

## 🔗 Integration Commands

### CI/CD Integration
```txt
CI Mode: Apply VOAI buckets + trigger full CI pipeline + quality gates + automated deployment on pass.
```

### Monitoring Integration  
```txt
Monitor Mode: Apply VOAI buckets + build + deploy + setup monitoring alerts + performance tracking.
```

---

## 📊 Reporting Templates

### Executive Summary
```txt
Report Mode: Apply VOAI buckets + generate executive summary + metrics dashboard + stakeholder communication.
```

### Technical Deep Dive
```txt
Technical Mode: Apply VOAI buckets + detailed technical report + architecture impact + developer documentation.
```

---

*Copy any template above and paste directly into Cursor for instant activation* ⚡

**Pro Tip:** Combine templates with `+` for multi-mode execution:
```txt
Performance Mode + Brand Mode: Apply VOAI Performance buckets + brand validation + optimized PR.
```