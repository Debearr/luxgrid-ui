{
  "name": "luxgrid-ui",
  "version": "0.1.0",
  "description": "Shared React + Tailwind CSS component library for the LuxGrid ecosystem (AuraFX, NØID, Noidlux, PostPilot).",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "cursor:health": "node scripts/cursor-health-check.mjs",
    "cursor:dev": "npm run cursor:health && npm run dev",
    "cursor:scout": "node scripts/market-scout.js --role researcher --system-prompt prompts/system.txt --user-prompt prompts/user.txt --output reports/test.json",
    "cursor:validate": "node scripts/generate-validation-report.js reports/test reports/test.json",
    "cursor:dashboard": "npm run dev & sleep 3 && (xdg-open http://localhost:3000/dashboard/market || open http://localhost:3000/dashboard/market || start http://localhost:3000/dashboard/market)",
    "cursor:deploy": "npm run cursor:health && git add -A && git commit -m \"deploy: cursor market scout\" || echo \"No changes\" && git push"
  },
  "keywords": [
    "react",
    "tailwind",
    "ui-library",
    "design-system",
    "luxury"
  ],
  "author": "LuxGrid Team",
  "license": "MIT",
  "devDependencies": {
    "@storybook/react": "^8.0.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
