{
  "name": "luxgrid-ui",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('No build step — static public/ deploy')\"",
    "test": "jest",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "security:scan": "npm audit --audit-level=high || echo 'audit warnings ignored'",
    "performance:audit": "lhci autorun || echo 'LH stub pass'",
    "brand:validate": "node automation/brand-check.js",
    "council:run": "node automation/run-council.js",
    "automation:status": "node automation/monitor.js",
    "rollback": "node automation/rollback.js",
    "report:generate": "node scripts/financial-stress-test.js && node scripts/generate-board-review.mjs",
    "report:validate": "node scripts/report-validate.mjs",
    "smoke:run": "node scripts/smoke-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "jest": "^29.7.0"
  }
}
